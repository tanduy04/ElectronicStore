name: Net Deployment

on:
  push:
    branches: [production]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy using ssh
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASSWORD }}
        port: 22
        script: |
          cd source_api/ElectronicStore
          git fetch origin
          git checkout production
          git pull
          cd ElectronicStore.Api/ElectronicStore.Api
          docker compose up -d --build --no-deps api
          